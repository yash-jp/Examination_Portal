<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.examination_portal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Examee.ETestActivity"></activity>
        <activity android:name=".organizer.ResultActivity" />
        <activity android:name=".Examee.EResultActivity" />
        <activity android:name=".Examee.EGroupActivity" />
        <activity android:name=".Examee.EQuestionActivity" />
        <activity android:name=".organizer.QuestionActivity" />
        <activity android:name=".organizer.TestActivity" />
        <activity android:name=".organizer.GroupActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/network_security_config" />

        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>